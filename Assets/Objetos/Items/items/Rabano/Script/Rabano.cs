using UnityEngine;

public class Rabano : Items
{
    private void Awake()
    {
        itemName = "Rabano";
        itemTrowAngle = 50;
        itemTrowVelocity = 15f;
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
